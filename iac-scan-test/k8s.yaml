apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
spec:
  template:
    spec:
      hostNetwork: true
      containers:
      - name: api
        image: myapp:latest
        securityContext:
          privileged: true
        ports:
        - containerPort: 22
